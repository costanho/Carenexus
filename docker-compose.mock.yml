services:
  mock-server:
    build:
      context: ./frontend/mock-server
      dockerfile: Dockerfile
    container_name: carenexus-mock
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
